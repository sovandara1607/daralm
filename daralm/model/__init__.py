"""Model architecture and configuration schema for DaraLM.

Phase 3: `config.py` (schema, since Phase 0) + `embeddings.py`,
`normalization.py`, `attention.py`, `feedforward.py`, `transformer_block.py`,
`transformer.py` — a from-scratch, config-driven, decoder-only Transformer
with RMSNorm, RoPE, causal self-attention, and tied embeddings. See
`transformer.py`'s module docstring for the full data flow.
"""
